package com.aforo.billablemetrics.enums;

public enum AggregationWindow {
    PER_EVENT,
    PER_MINUTE,
    PER_HOUR,
    PER_DAY,
    PER_WEEK,
    PER_MONTH,
    PER_QUERY,
    PER_DELIVERY,
    PER_FILE
}
